# UI Issues v9 — Een correcte userstory gif voor labelling maken.
us-2b.1
## Segment view
The gif in segment view still shows filling in the config.yaml and the categories yaml from 0:00 to 1:18.
It should only show:

A. (in the future it should show the ./start running that directly goes to filling this receipt, but that is not yet feasible, so ignore this).
<!-- A. the "--edit-receipt command, -->
<!-- B. The selection of the receipt. -->
C. The can you see the receipt question
D. the filling of the WHOLE receipt (not just changing the category). 
E. and how the receipt is stored."
F. The receipt image does not contain the address that is in the receipt label. It should. Same for the tax amount. 

## Full view
The full view 
Should not show the starting journal for this userstory, 
it should show the cat of the bank csv transaction
For the editing it should use C.D.E of Segment view. 
It should show the matching algoriothm and outcome (with the recursive matching algorithm diagram )
it should show the journal output.